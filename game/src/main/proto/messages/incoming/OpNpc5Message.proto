syntax = "proto3";
import "options.proto";

option java_package = "gg.rsmod.proto.messages.incoming";
option java_multiple_files = true;

message OpNpc5Message {
    option (rsmod.header).opcode = 31;
    option (rsmod.header).type = Fixed;

    int32 movement_type = 1 [(rsmod.type) = Byte];
    int32 index         = 2 [(rsmod.type) = Short, (rsmod.trans) = Add];
}