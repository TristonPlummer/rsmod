syntax = "proto3";
import "options.proto";

option java_package = "gg.rsmod.proto.messages.incoming";
option java_multiple_files = true;

message OpLoc2Message {
    option (rsmod.header).opcode = 79;
    option (rsmod.header).type = Fixed;

    int32 movement_type = 1 [(rsmod.type) = Byte, (rsmod.trans) = Add];
    int32 id            = 2 [(rsmod.type) = Short];
    int32 z             = 3 [(rsmod.type) = Short];
    int32 x             = 4 [(rsmod.type) = Short];
}