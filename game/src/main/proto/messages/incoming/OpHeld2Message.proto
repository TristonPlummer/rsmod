syntax = "proto3";
import "options.proto";

option java_package = "gg.rsmod.proto.messages.incoming";
option java_multiple_files = true;

message OpHeld2Message {
    option (rsmod.header).opcode = 98;
    option (rsmod.header).type = Fixed;

    int32 item              = 1 [(rsmod.type) = Short, (rsmod.trans) = Add];
    int32 component_hash    = 2 [(rsmod.type) = Int, (rsmod.order) = InverseMiddle];
    int32 slot              = 3 [(rsmod.type) = Short, (rsmod.trans) = Add, (rsmod.order) = Little];
}