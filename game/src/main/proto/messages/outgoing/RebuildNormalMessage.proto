syntax = "proto3";
import "options.proto";

option java_package = "gg.rsmod.proto.messages.outgoing";
option java_multiple_files = true;

message RebuildNormalMessage {
    option (rsmod.header).opcode = 0;
    option (rsmod.header).type = VariableShort;

    int32 z     = 1 [(rsmod.type) = Short, (rsmod.trans) = Add, (rsmod.order) = Little];
    int32 x     = 2 [(rsmod.type) = Short, (rsmod.trans) = Add];
    bytes xteas = 3 [(rsmod.type) = Bytes];
}